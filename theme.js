// Text Container Styles
const textContainerStyles = Object.freeze({ 
    maxWidth: '9rem', 
    maxHeight: '6rem', 
    overflow: 'scroll',
    minHeight: '8rem'
})

// Close Button Styles
const closeButtonStyles = Object.freeze({ 
    backgroundImage: "url('https://img.icons8.com/windows/32/000000/multiply.png')",
    minHeight: '2rem',
    minWidth: '2rem',
})

// Note Element Styles
const noteElementStyles = Object.freeze({
    backgroundImage: "url('./assets/notebg.png')",
    maxHeight: '16rem',
})